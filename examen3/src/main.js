import { greet } from "./assets/js/utilis.js";

greet("Lilia Ould Hocine");

const liensHTML = document.querySelectorAll(".nav-header-link");

//Debut de la navigaiton sans chargement
/* liensHTML.forEach(lien => {
  lien.addEventListener("click", intermediaire => {
    intermediaire.preventDefault();
  });
}); */
